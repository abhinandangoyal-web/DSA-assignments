#include <iostream> using namespace std;

int findMissingLinear(int arr[], int n) {
int total = (n + 1) * (n + 2) / 2; // sum of 1 to n+1 for (int i = 0; i < n; i++) {
total -= arr[i];
}
return total;
}
